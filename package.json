{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "test": "jest",
    "coverage": "jest --coverage",
    "codecov": "codecov"
  },
  "devDependencies": {
    "codecov": "^3.8.3",
    "jest": "^27.5.1"
  }
}
